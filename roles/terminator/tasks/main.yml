---
# ################################################################

- name: " "
  ansible.builtin.include_tasks: "{{ dist_family }}.yml"
  when:
    - dist_family in [ 'debian' ]
    - is_gui
  tags:
    - "terminator"

#################################################################
