---
# ################################################################

- name: "Apt > install"
  ansible.builtin.apt:
    name: "{{ dbeaver.libraries.apt }}"
    state: "latest"
  tags:
    - "{{ dbeaver.tags }}"
    - "dbeaver"

#################################################################

- name: "Apt > download and install deb"
  ansible.builtin.apt:
    deb: "{{ dbeaver.url }}"
  tags:
    - "{{ dbeaver.tags }}"
    - "dbeaver"

#################################################################

- name: "Launcher > desktop icon"
  ansible.builtin.template:
    src: "{{ desktop }}.desktop.j2"
    dest: "/usr/share/applications/{{ desktop }}.desktop"
    owner: "{{ primary_user.id }}"
    group: "{{ primary_user.id }}"
    mode: "0644"
  tags:
    - "{{ dbeaver.tags }}"
    - "launcher"

- name: "Launcher > add"
  become: true
  become_user: "{{ primary_user.id }}"
  ansible.builtin.script: "../../../scripts/add_{{ desktop_environment }}_launcher.sh {{ desktop }}"
  changed_when: false
  tags:
    - "{{ dbeaver.tags }}"
    - "launcher"

#################################################################
